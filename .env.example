MARKFILL_PORT=4810
SOL_RPC=https://api.mainnet-beta.solana.com
# Optional — Hermes price updates. Tape still works without it (Yahoo + Jupiter).
PYTH_API_KEY=
# Master seed used to create each user's desk wallet (base58). Keep this private.
MARKFILL_SECRET=
