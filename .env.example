MARKFILL_PORT=4810
SOL_RPC=https://api.mainnet-beta.solana.com
# Optional — Hermes price updates. Tape still works without it (Yahoo + Jupiter).
PYTH_API_KEY=
# Master seed for per-user desk wallets. Set this ONCE on Render and never change it
# or every user's deposit address would change. Leave empty only on first local run.
MARKFILL_SECRET=
